# User Management

Простой бэкенд на Spring Boot для управления пользователями (базовая часть системы бронирования). Реализует REST API для создания, чтения, обновления и удаления пользователей (CRUD).

## 🛠 Технологии

* **Java** (JDK 17+)
* **Spring Boot 3+** (Web, Data JPA)
* **Hibernate** (ORM)
* **PostgreSQL** (или другая SQL база данных, настроенная в `application.properties`)
* **Maven** (Сборка проекта)

## 🚀 Функционал

* Регистрация пользователей с автоматическим расчетом возраста по дате рождения.
* Валидация уникальности Email.
* Обновление данных пользователя (Имя, Email) через Transactional (Dirty Checking).
* Удаление пользователей.

## ⚙️ Запуск проекта

1.  Убедитесь, что у вас установлена Java и Maven.
2.  Настройте подключение к базе данных в `src/main/resources/application.properties`.
3.  Запустите приложение командой:

```bash
mvn spring-boot:run
